import React from 'react';
import styled from 'styled-components';

export default function Header() {
  return <Container>force.</Container>;
}

const Container = styled.div`
  height: 60px;
  margin-left: 20px;
  font-size: 3.6rem;
`;
